#include "main.h"

/**
 * print_triangle - prints a triangle
 * @size - size of the triangle
 *
 * Return: void
 */

void print_triangle(int size)
{
	if (size == 0)
	{
		_putchar('\');
	}

	else
	{

	}
}
