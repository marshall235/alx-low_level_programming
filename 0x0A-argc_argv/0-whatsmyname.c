#include "main.h"
#include <stdio.h>

/**
 * main - prints a programs name followed by a new line
 * @argc: argument count
 * @argv: argument vector
 * Return: is 0
 */

int main(argc, char *argv[])
{
	(void) argc;

	printf("%\n", argv[0]);

	return (0);
}
