#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *
 * Return: is zero
 */

int _abs(int)
{
	if (n < 0)
	{
		n *= -1;

		_putchar('n');
	}

	else
	{
		_putchar('n');
	}

	return (0);
}
