#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: number to be checked
 *
 * Return: is zero
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;

		_putchar('n');
	}

	else
	{
		_putchar('n');
	}

	return (0);
}
